{
  "buildpacks": [
    { "url": "heroku/python" }
  ],
  "environments": {
    "test": {
      "addons": [
        "heroku-postgresql:in-dyno"
      ],
      "scripts": {
        "pre-test": "echo MOSH; echo $DATABASE_URL;",
        "test": "pytest -s -v"
      },
      "env": {
        "DJANGO_SETTINGS_MODULE": "storefront.settings.test"
      }
    }
  }
}