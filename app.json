{
  "buildpacks": [
    { "url": "heroku/python" }
  ],
  "environments": {
    "test": {
      "addons": [
        "cleardb:ignite"
      ],
      "scripts": {
        "test": "pytest -v"
      },
      "env": {
        "DJANGO_SETTINGS_MODULE": "storefront.settings.test"
      }
    }
  }
}