{
  "buildpacks": [
    { "url": "heroku/python" }
  ],
  "environments": {
    "test": {
      "addons": [
        "cleardb:ignite"
      ],
      "scripts": {
        "test": "pytest --reuse-db"
      },
      "env": {
        "DJANGO_SETTINGS_MODULE": "storefront.settings.test"
      }
    }
  }
}